spring:
    application:
        name: registry-service
    cloud:
        config:
            enabled: true
            uri: http://localhost:8081
#eureka:
#    instance:
#        hostname: localhost